# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local SSO client id override / callback port - never commit real values,
# only src-tauri/.env.example (placeholders) is meant to be tracked.
.env
src-tauri/.env

# Local-only reference clones of other projects' repos (eve-nexum, Pathfinder,
# evemon, wanderer, zKillboard, EveLens, LazyBlacksmith, eve-tools-suite) used
# for source-verification research - not part of VESPER itself.
Git sources/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
